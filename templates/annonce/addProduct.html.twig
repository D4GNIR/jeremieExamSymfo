{% extends 'base-front.html.twig' %}

{% block title %}Créer un Produit{% endblock %}

{% block body %}
<div class="container">
    <div class="col-12">
        {{ form_start(form) }}
            <div class="row">
                <div class="col-6">
                    {{ form_row(form.name) }}
                </div>
                <div class="col-6">
                    {{ form_row(form.imagePath) }}
                </div>
            </div>
        </div>
        {{ form_end(form) }}
    </div>
</div>


{% endblock %}